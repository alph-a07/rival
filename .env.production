# Production environment (mode = production).
# Loaded by Vite for `npm run build` / `vite preview`. Committed.

# Provisioning environment — drives log verbosity in src/core/logging/config.ts.
VITE_ENV=production
